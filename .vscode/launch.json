{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Angular App",
            "request": "launch",
            "command": "ng serve --port 8000",
            "cwd": "${workspaceFolder}"
        }
    ]
}